/** Max planning applications shown, enriched, and included in AI summary per digest run. */
export const DIGEST_EMAIL_MAX_LEADS = 5;

/** Cap ICP classifications per digest run to control AI spend. */
export const DIGEST_ICP_SCORE_CAP = 15;

/** Max job_estimator calls for auto-outreach workflows per digest run. */
export const OUTREACH_ESTIMATE_CAP = 5;
