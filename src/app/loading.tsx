export default function RootLoading() {
  return (
    <div className="flex min-h-screen flex-col bg-zinc-50">
      {/* Minimal placeholder — the RouteProgress bar handles the visual feedback */}
      <div className="flex-1" />
    </div>
  );
}
